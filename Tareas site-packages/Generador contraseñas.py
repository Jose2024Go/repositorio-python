import tkinter as tk
from tkinter import messagebox
from PIL import ImageTk, Image
from tkinter.filedialog import askopenfilename

ventana = tk.Tk()
ventana.title("Biblioteca virtual")

ventana.geometry("1060x566")
fondo = ImageTk.PhotoImage(file = 'C:\\CURSOS PYTHON\\ejercicios\\pythonProject1\\sistemas.gif')  # Corrección: cambia 'Inicio' a 'file'
fondo1 = tk.Label(ventana, image = fondo)
fondo1.place(x=0, y=0)
barra_menu = tk.Menu(ventana)
ventana.config(menu = barra_menu)

def salir():
    resp = messagebox.askquestion("Salir","Desea salir?")
    if resp == 'yes':
        ventana.destroy()

def ventana_añadir():
    v1 = tk.Toplevel(ventana)
    v1.title("Añadir libro a la bibioteca")
    titulo1=("Titulo")
    v1.geometry("250x250")
    def libroBiblioteca():
        titulo2 = (txt1.get())
        titulo3 = (txt2.get())
        titulo4 = (txt3.get())
        titulo5 = int(txt4.get())
        messagebox.showinfo("Resultado",f"*El titulo: {titulo2}*Autor nombre:{titulo3}*Autor apellido:{titulo4}*ISBN del libro:{titulo5}")
    text1 = tk.Entry(v1)
    text2 = tk.Entry(v1)
    text3 = tk.Entry(v1)
    text4 = tk.Entry(v1)
    btn1 = tk.Button(v1, text = "mostrar", command = libroBiblioteca)
    text1.pack()
    text2.pack()
    text3.pack()
    text4.pack()
    btn1.pack()

def acerca_de():
    messagebox.askquestion("Acerca de", "Desarrollado por Daniel, Jose, Anthony, Darwin, Jhon y James")


Inicio = tk.Menu(barra_menu, tearoff=0)
barra_menu.add_cascade(label = "Inicio",menu =Inicio)
Inicio.add_command(label = "Cerrar",command = salir)

Opciones = tk.Menu(barra_menu, tearoff = 0)
barra_menu.add_cascade(label = "Opciones", menu = Opciones)
Opciones.add_command(label = "Añadir libro a la bibioteca", command =ventana_añadir)
Opciones.add_cascade(label = "Mostrar biblioteca")
Opciones.add_cascade(label = "Borrar libro")
Opciones.add_cascade(label = "¿Numero de libros?")

Ayuda = tk.Menu(barra_menu, tearoff = 0)
barra_menu.add_cascade(label = "Ayuda",menu = Ayuda)
Ayuda.add_command(label = "Hecho por",command = acerca_de)

ventana.mainloop()