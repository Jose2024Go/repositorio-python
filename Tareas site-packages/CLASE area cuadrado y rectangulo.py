class Rectangulo:
    def _initRentangulo(self):
        self.base = ''
        self.altura = ''

    def leer(self):
        self.base = input('digite la base del rectangulo')
        self.altura = input('digite el tamaño de altura del rectangulo')

    def mostrar(self):
        print(self.base,self.altura)

    def area(self):
        return (self.base * self.altura)

    def comparar (self, a, area):
        if a == area:
            print('son iguales')
        else:
            print('son diferentes')

    class Cuadrado:
        def __init_Cuandrado(self):
            self.lado = ''
        def leer(self):
            self.lado = input('digite el lado de cuadrado')
        def mostrar(self):
            print(self.lado)
        def area(self):
            return (self.lado * self.lado)
    cuadrado1 = Cuadrado()
    rectangulo1 = Rectangulo()
    cuadrado1.leer()
    rectangulo1. leer()
    cuadrado1. mostrar()
    rectangulo1. mostrar()
    Rectangulo. comparar()